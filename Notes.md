*** SRC Boilerplate ***

-- In the index.js file, <App/> component is surrounded by <React.StrictMode> tags.
-- Essentially strict mode is a tool for highlighting potential problems. It activates additional checks and warnings for its descendants.
-- It only runs in development. It will not affect your production build.However, it runs twice in development because it wraps the component which is the main <App/>.

-- index.js will be the file where we're going to inject our javascript logic.
-- app.js is our root component.

*NOTE*
-- On some computers, people use Control/Command + D to choose a specific word in the whole file at once. Since this combination works as a duplication function, I have to go with "Command+Shift+L". 

